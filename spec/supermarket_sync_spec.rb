require 'spec_helper'

describe SupermarketSync do
  it 'has a version number' do
    expect(SupermarketSync::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
